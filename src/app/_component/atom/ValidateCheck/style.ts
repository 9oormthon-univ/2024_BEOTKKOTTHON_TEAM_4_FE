import { Colors, fontGenerator } from '@/styles';
import styled from '@emotion/styled';
import ValidateCheck from '@/app/_component/atom/ValidateCheck/index';

export const ValidateCheckWrap = styled.div``;
