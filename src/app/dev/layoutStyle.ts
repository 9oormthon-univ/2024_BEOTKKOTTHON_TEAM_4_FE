import styled from "@emotion/styled";

export const DevLayoutMain = styled.main`
  width:100vw;
    display: flex;
    justify-content: flex-start;
  position: absolute; /* 절대 위치 설정 */
  left: 0;

`;
